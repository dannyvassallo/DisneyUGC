$('.sort_paginate_ajax').html("<%= escape_javascript(render("posts"))%>");
$('.feed-modal-trigger').leanModal({
	complete: function() {
	    $('video').each(function(){ 
	      this.player.pause(); 
	    });
	} 
});